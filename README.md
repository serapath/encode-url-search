# encodeURLsearch
encode search query

## example

```js
  var encodeURLsearch = require('encode-url-search')
  var search = encodeURLsearch({ emse: 'kremse' })
  console.log(search)
  // => "?emse=kremse
```
